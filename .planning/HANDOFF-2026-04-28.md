# Handoff for new Claude Code session — 2026-04-28

This document is for the Claude session opening a fresh context window
to continue work on the MailBox One appliance project. Read this first,
then read the files it points you at.

## Repo orientation

You are in `~/mailbox` on the Ubuntu workstation. Three locations stay
in sync via git: this workstation (`~/mailbox`), the Jetson appliance
(`bob@10.42.0.2:~/mailbox` via SSH alias `jetson`), and GitHub
(`github.com:ConsultingFuture4200/mailbox.git`).

After every commit on the workstation, sync the Jetson:

    git push origin master
    ssh jetson 'cd ~/mailbox && git pull'

The Jetson runs the live appliance: Postgres, n8n, Ollama, Qdrant,
Caddy, dashboard. SSH alias `jetson` resolves to `10.42.0.2` (direct
ethernet); fallback `jetson-wifi` to `192.168.1.45`. See `CLAUDE.md`
"Deployment Target" for details.

## Read these in order before doing anything

1. `.planning/STATE.md` — authoritative project state. Read all of it.
2. `CLAUDE.md` — project governance, deployment target, conventions.
3. `.planning/phases/02-email-pipeline-core/02-CONTEXT.md` — Phase 2
   context and decisions D-01..D-24.
4. `.planning/phases/02-email-pipeline-core/02-CONTEXT-ADDENDUM-v2-2026-04-27.md`
   — decisions D-25..D-49 from the 2026-04-27 Phase 2 re-scope. This
   is where most architectural calls live. Append-only.
5. `.planning/phases/02-email-pipeline-core/02-03-imap-ingestion-watchdog-SUMMARY-v1-2026-04-28.md`
   — the most recently completed work. Reflects what actually shipped
   vs what was originally scoped.
6. `.planning/phases/02-email-pipeline-core/02-04-classification-routing-PLAN-v2-2026-04-27-STUB.md`
   — the next plan in pipeline order. THIS IS YOUR RESUME POINT.

## Current execution mode: lean

Per STATE.md "Path B" decision: stubs are authoritative spec, code is
written directly against them, SUMMARY.md is written after work lands.
Do NOT promote stubs to full v2 plans as separate artifacts before
executing. The 2026-04-28 attempt to promote 02-03's stub to a full
plan was discarded in favor of writing a SUMMARY.md retrospectively
(see commit c2b898b and the SUMMARY file).

## What's done in Phase 2

- 02-01: SUPERSEDED (frontmatter marker)
- 02-02 v2: COMPLETE (6 SQL migrations + types/queries shipped)
- 02-03: PARTIAL — schema migration 007 applied; MailBOX workflow
  extended for thread-header capture; reply email validated end-to-end.
  See SUMMARY for what got deferred.
- 02-04..08: stubs written, decisions resolved (D-25..D-49), execution
  pending.

## Live appliance state (verify before assuming)

These commands give ground truth in <30 seconds; run them at session
start before making any plans or edits.

    ssh jetson "cd ~/mailbox && docker compose exec -T postgres psql -U mailbox -d mailbox -Atc \
      \"SELECT version FROM mailbox.migrations ORDER BY version;\""

    ssh jetson "cd ~/mailbox && docker compose exec -T postgres psql -U mailbox -d mailbox -Atc \
      \"SELECT name, active FROM workflow_entity ORDER BY name;\""

    ssh jetson "cd ~/mailbox && docker compose exec -T postgres psql -U mailbox -d mailbox -Atc \
      \"SELECT
         (SELECT COUNT(*) FROM mailbox.inbox_messages) AS inbox,
         (SELECT COUNT(*) FROM mailbox.classification_log) AS class_log,
         (SELECT COUNT(*) FROM mailbox.drafts) AS drafts;\""

    git log --oneline -10

Expected at handoff time:
- Migrations 001-007 applied
- Workflows: MailBOX, MailBOX-Drafts, MailBOX-Send all active
- ~8+ inbox_messages rows (6 historical + recent dogfood activity);
  the id sequence has jumped substantially due to failed Insert
  experiments — this is cosmetic
- 0 rows in classification_log (02-04 not yet shipped)
- 2 rows in drafts (from Phase 1 sub-project NIM-classified runs)

## What 02-04 needs to do (per the stub + carried-forward items)

The 02-04 stub captures the architectural shape. Carried-forward items
from the 02-03 SUMMARY:

- Migrate classifier from legacy taxonomy
  (action_required/informational/personal/spam/test) to
  MAIL-05 8 categories (inquiry, reorder, scheduling,
  follow_up, internal, spam_marketing, escalate, unknown).
- Move classifier prompt to canonical source
  (dashboard/lib/classification/prompt.ts + internal API endpoint)
  per D-29.
- Filter dupes before classify (Fix C from session log) — only
  classify rows actually new to the DB; reduces wasted Ollama calls.
- Schedule frequency: trigger still fires every 1 minute. UI rename
  attempt did not persist. May need to edit n8n's database directly
  or recreate the trigger node fresh.
- Wire classification_log writes (table exists from 02-02-v2
  migration 002 but is empty).
- Per D-32: set auto_send_blocked=true on mailbox.drafts rows
  for escalate category (Phase 3 enforces it).
- Per D-31: spam emails get classification_log row but NO drafts row.
- Per D-49: lib/onboarding/live-gate.ts boundary; classification
  fires unconditionally, drafting blocked until live or tuning bypass.

## Surprise gotchas from prior session work

These are things the new Claude session will trip over if not warned:

1. NVIDIA NIM, not Anthropic Haiku. The existing MailBOX-Drafts
   workflow uses NVIDIA NIM API for cloud drafting (meta/llama-3.3-70b-instruct).
   This is NOT what the v2 stubs assumed. Treat as dogfood scaffolding
   that 02-07 will replace. NIM may end up being the production cloud
   path instead of Haiku — decision deferred.

2. n8n Postgres node Insert mode is comma-safe; Execute Query is not.
   The original Execute Query mode parsed parameters by splitting
   queryReplacement on commas, which silently broke on email bodies
   containing commas. Use Insert mode with column mapping for any
   new Postgres writes from n8n.

3. n8n expression mode and the = prefix. When typing in an
   Expression-mode field in n8n's UI, do NOT type the leading =.
   The = is implicit; typing it produces values like ={{ ... }}
   that work for string columns but break for numeric columns
   (we hit this with confidence).

4. Gmail node returns thread headers as top-level camelCase fields:
   $json.inReplyTo and $json.references (NOT under $json.headers).
   First emails in a thread have these as undefined, which n8n correctly
   converts to NULL on insert.

5. Schedule Trigger UI quirk. Renaming the node does not change
   the trigger interval. The interval lives in the rule.interval[].minutesInterval
   field which the UI sometimes fails to persist. If you change the
   interval, verify by querying n8n's workflow_entity table directly
   and reading nodes::jsonb.

6. Workflow JSON drift. The committed n8n/workflows/01-email-pipeline-main.json
   reflects the live MailBOX workflow as of 2026-04-28. There is no
   automated import/export — operator edits the workflow in n8n's UI,
   then re-exports the JSON, then commits. Drift is detectable manually
   but not automated.

## Tools and conventions

- Migration runner: ephemeral Docker service mailbox-migrate,
  invoked via `docker compose --profile migrate run --rm mailbox-migrate`.
  Runs forward-only SQL files from dashboard/migrations/ in lexical
  order. Tracks applied versions in mailbox.migrations.
- Filename versioning: per user preference, all migration and plan
  files include version-history suffixes (-v1-YYYY-MM-DD.sql).
- Commit messages: Conventional Commits style with feat(plan-id)
  or docs(plan-id) scope. Body explains the why and references the
  decision IDs from the addendum where relevant.
- Stub vs PLAN vs SUMMARY: Stubs (suffix -STUB.md) capture shape
  + decisions, are not executable. SUMMARYs (-SUMMARY-v1-YYYY-MM-DD.md)
  document what shipped. PLAN files in original *-PLAN.md form are
  v1 historical artifacts, mostly superseded.

## Personal context

Dustin is the operator. White-glove onboarding model — every customer
launch involves a real human (Dustin) walking through setup. This
biases architectural choices toward "operator-configures-once" rather
than "self-serve UX" (see D-47).

Working hours pattern: Dustin frequently works late, sometimes past
midnight. Long sessions accumulate fatigue and confusion; new context
windows are explicitly used to reset state. This handoff document is
to make that reset cheap.

## When in doubt

- Verify before assuming. The live system has surprised us multiple
  times. Run the ground-truth queries before designing.
- Ask before doing destructive things. The Jetson is a single-instance
  appliance with snapshots but no automated rollback for n8n state.
- The addendum (02-CONTEXT-ADDENDUM-v2-2026-04-27.md) is your friend.
  When you don't know what choice to make on a cross-plan issue, look
  there first — there's a real chance it's already decided.
