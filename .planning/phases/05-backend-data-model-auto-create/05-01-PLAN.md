---
phase: 05-backend-data-model-auto-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql
  - dashboard/test/fixtures/schema.sql
  - dashboard/lib/db/schema.ts
  - dashboard/test/schema-invariants.test.ts
  - dashboard/test/lib/job-outcomes.test.ts
autonomous: false
requirements: [MAP-01, FILT-05]

must_haves:
  truths:
    - "mailbox.accounts has a nullable business_id column referencing mailbox.businesses(id) ON DELETE SET NULL (MAP-01, D-05 repairability)"
    - "Deleting a business leaves its linked account rows intact with business_id set to NULL — never cascades (MAP-01)"
    - "Every row in mailbox.businesses carries a non-null, globally unique slug after migration 057 applies (FILT-05, D-13)"
    - "The business named 'Altitude Guitar' carries slug 'altitude', matching the single live jobs.json reference (FILT-05, D-14)"
    - "dashboard/test/fixtures/schema.sql bootstraps a database whose accounts/businesses shape is byte-equivalent to what migration 057 produces"
    - "dashboard/lib/db/schema.ts (kysely-codegen output) contains accounts.business_id, so Kysely writes to that column typecheck"
  artifacts:
    - dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql
    - "dashboard/test/fixtures/schema.sql — trailing '── Migration 057 ──' block"
    - "dashboard/lib/db/schema.ts — regenerated, includes business_id on the Accounts interface"
    - "dashboard/test/schema-invariants.test.ts — new cases for business_id FK semantics and slug NOT NULL/UNIQUE"
  key_links:
    - "migration 057 DDL ↔ test/fixtures/schema.sql tail block — if these diverge, CI passes green while the live DB and the generated types disagree"
    - "test/fixtures/schema.sql ↔ dashboard/lib/db/schema.ts — codegen reads the fixture, not migrations/; a missed db:codegen makes plan 05-02's Kysely write fail to compile"
    - "businesses.slug NOT NULL ↔ every existing INSERT into mailbox.businesses — any writer that omits slug starts throwing 23502"
  prohibitions:
    - "MUST NOT constrain accounts.business_id NOT NULL — MAP-04 requires unmatched accounts to stay unlinked, and D-05 requires a failed auto-link to leave the column null"
    - "MUST NOT inline the business_id FK into the mailbox.accounts CREATE TABLE block in test/fixtures/schema.sql (line ~1228) — mailbox.businesses is not defined until ~1599 and bootstrap would fail (PATTERNS conflict #1)"
    - "MUST NOT implement slug as a Postgres GENERATED ALWAYS AS (...) STORED column — that recomputes on rename and violates D-12's frozen-at-creation guarantee"
    - "MUST NOT seed the 11 sidecar ENTITY_OPTIONS slugs (heron, state, cde, krunchy, yes, future, umb, glue, myco, personal, unsorted) — they are Heron Labs' customer list, not Mike's businesses (D-15)"
    - "MUST NOT rewrite, move, or otherwise touch ~/.hermes/cron/jobs.json — this phase is DB-side only (D-14)"
    - "MUST NOT edit any already-applied migration file (052-056) — corrections ship as a new numbered file"
---

<objective>
Land the schema half of Phase 5: migration 057 adds the nullable `mailbox.accounts.business_id`
FK (MAP-01) and the `mailbox.businesses.slug` NOT NULL UNIQUE column with its computed backfill
plus the single legacy `altitude` seed (FILT-05, D-13, D-14). Mirror the identical DDL into
`dashboard/test/fixtures/schema.sql`, regenerate `dashboard/lib/db/schema.ts` via kysely-codegen,
and add schema-invariant coverage proving the FK's `ON DELETE SET NULL` behavior and the slug
constraints hold.

Purpose: every later plan in this phase writes to `accounts.business_id` and reads
`businesses.slug`. Nothing downstream compiles or runs correctly until this column exists in
all three places the repo tracks schema (live migration, fixture snapshot, generated Kysely
types). This repo's documented failure mode is exactly here — CI applies the fixture, not
`migrations/`, and CI does not run `db:codegen:verify` — so a partial sync ships silently
broken.

Output: one migration file, one fixture-schema tail block, a regenerated codegen artifact, and
extended schema-invariant + job-outcomes test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-data-model-auto-create/05-CONTEXT.md
@.planning/phases/05-backend-data-model-auto-create/05-PATTERNS.md
@CLAUDE.md

Read these source files before writing anything:
@dashboard/migrations/053-crm-businesses-v1-2026-06-05.sql
@dashboard/migrations/033-add-account-id-multi-account-v1-2026-05-28.sql
@dashboard/migrations/runner.ts
</context>

<artifacts_this_plan_produces>
**Files created**
- `dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql`

**Files modified**
- `dashboard/test/fixtures/schema.sql` (append a `-- ── Migration 057 ... ──` block at EOF)
- `dashboard/lib/db/schema.ts` (regenerated by `npm run db:codegen` — do not hand-edit unless the Docker fallback in Task 2 is invoked)
- `dashboard/test/schema-invariants.test.ts` (new `it()` cases)
- `dashboard/test/lib/job-outcomes.test.ts` (two raw INSERTs gain a slug value)

**DB columns created**
- `mailbox.accounts.business_id` — `INTEGER NULL REFERENCES mailbox.businesses(id) ON DELETE SET NULL`
- `mailbox.businesses.slug` — `TEXT NOT NULL`

**DB indexes created**
- `businesses_slug_key` — `CREATE UNIQUE INDEX IF NOT EXISTS businesses_slug_key ON mailbox.businesses (slug)`

**Migration version string registered in `mailbox.migrations`**
- `057-add-accounts-business-id-and-business-slug-v1-2026-07-28`

**Data seeded**
- `mailbox.businesses.slug = 'altitude'` on the row where `name = 'Altitude Guitar'` (exactly one row, D-14)
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 057 — accounts.business_id FK + businesses.slug with computed backfill and legacy seed</name>

  <read_first>
    - `dashboard/migrations/053-crm-businesses-v1-2026-06-05.sql` — the exact `departments.business_id` FK shape to mirror, and the current `mailbox.businesses` DDL
    - `dashboard/migrations/033-add-account-id-multi-account-v1-2026-05-28.sql` — the header-comment convention when a migration carries DML, and the `DO $$ ... $$` add→backfill→constrain idiom
    - `dashboard/migrations/runner.ts` (lines 20-45) — confirms version = filename minus `.sql`, lexical sort, and that each file runs inside one `BEGIN`/`COMMIT`
    - `CLAUDE.md` § "Comment standard (migrations)"
  </read_first>

  <files>dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql</files>

  <action>
Create the migration file at exactly that path. `runner.ts` derives the version from the
filename and sorts lexically, so `057-` must be the next number — 056 is the current maximum
(verified). Per the CLAUDE.md landmine, several existing headers cite stale numbers after the
PR #239 renumbering: trust the filename and write `057` in the header.

Header block (this migration carries DML, so it needs a BACKFILL section beyond the standard
WHAT / WHY / ROLLBACK — follow migration 033's precedent):

- `-- Migration 057 — M5 Unified Entities: account→business FK + frozen business slug.`
- `-- WHAT:` adds a nullable `mailbox.accounts.business_id` FK, adds `mailbox.businesses.slug`
  (computed for existing rows, then constrained NOT NULL + UNIQUE), and seeds the one legacy
  slug value that a live cron job still references.
- `-- WHY:` M5 / Phase 5 — MAP-01 (account↔business mapping) and FILT-05 (slug back-compat so
  the `agentbox3` cron job carrying the string `altitude` keeps resolving once Phase 7 repoints
  the sidecar at the CRM). Per D-16 the runtime attach rule is sibling-account-domain based;
  this migration adds only the storage, not the rule.
- `-- ROLLBACK:` `DROP INDEX IF EXISTS mailbox.businesses_slug_key; ALTER TABLE
  mailbox.businesses DROP COLUMN IF EXISTS slug; ALTER TABLE mailbox.accounts DROP COLUMN IF
  EXISTS business_id;` then delete the row from `mailbox.migrations`.
- `-- BACKFILL:` state that `business_id` is deliberately left NULL for every existing row —
  the account→business linkage backfill runs as a separate idempotent Node script
  (`npm run business:backfill`, plan 05-03) so it uses byte-identical resolution logic to the
  runtime hook (D-10, "one rule, exercised twice"). State that only `slug` is backfilled here,
  by a deterministic SQL transform, and list the six expected name→slug pairs for the live
  `agentbox3` data as an in-header parity record:
  `Altitude Guitar → altitude-guitar` (then overridden to `altitude` by the D-14 seed),
  `UMB Advisors → umb-advisors`, `AutoCSR → autocsr`, `Jiffy Auto Glass → jiffy-auto-glass`,
  `Elevated Advisory → elevated-advisory`, `Bonvillian Design → bonvillian-design`.
  Add the cross-reference line: this SQL transform must stay byte-equivalent to
  `dashboard/lib/crm/auto-link.ts:generateSlug()` (built in plan 05-02); a DB-gated parity test
  in `dashboard/test/lib/crm-auto-link.test.ts` asserts the two agree.

Statement order inside the file (all inside the runner's single transaction):

1. `ALTER TABLE mailbox.accounts ADD COLUMN IF NOT EXISTS business_id INTEGER REFERENCES
   mailbox.businesses(id) ON DELETE SET NULL;` — copy migration 053's `departments.business_id`
   shape verbatim. Deliberately nullable and never constrained (see prohibitions).
2. `ALTER TABLE mailbox.businesses ADD COLUMN IF NOT EXISTS slug TEXT;` — nullable at first.
3. Base backfill for rows still missing a slug. Lowercase, collapse every run of
   non-alphanumeric characters to a single hyphen, trim leading/trailing hyphens, and fall back
   to the literal `business` when the transform yields an empty string:
   `UPDATE mailbox.businesses SET slug = COALESCE(NULLIF(lower(regexp_replace(regexp_replace(name, '[^a-zA-Z0-9]+', '-', 'g'), '^-+|-+$', '', 'g')), ''), 'business') WHERE slug IS NULL;`
4. Collision suffixing via a `DO $$ ... $$` PL/pgSQL block (D-13). Iterate rows whose computed
   slug duplicates an earlier row's (`EXISTS (SELECT 1 FROM mailbox.businesses o WHERE o.slug =
   b.slug AND o.id < b.id)`) ordered by `id`; for each, probe `slug || '-' || n` starting at
   `n = 2`, incrementing until the candidate is unused, then UPDATE that row. Keep this
   self-contained SQL — no Node/tsx dependency at migration-apply time, because the migrate
   profile container has no path to invoke a script mid-`psql` run.
5. Legacy slug seed (FILT-05 / D-14) — exactly one row, guarded so it is idempotent and cannot
   create a duplicate:
   `UPDATE mailbox.businesses SET slug = 'altitude' WHERE name = 'Altitude Guitar' AND slug IS DISTINCT FROM 'altitude' AND NOT EXISTS (SELECT 1 FROM mailbox.businesses WHERE slug = 'altitude');`
   Placed after the collision loop and before the constraints, so if the guard is ever defeated
   the unique index creation fails loudly and the whole transaction rolls back rather than
   shipping a duplicate. Add a comment: this is the ONLY slug seed — D-14 verified the live
   `~/.hermes/cron/jobs.json` holds 4 jobs of which exactly one carries a business slug.
6. `ALTER TABLE mailbox.businesses ALTER COLUMN slug SET NOT NULL;`
7. `CREATE UNIQUE INDEX IF NOT EXISTS businesses_slug_key ON mailbox.businesses (slug);` — a
   unique index rather than `ADD CONSTRAINT`, because the index form supports `IF NOT EXISTS`
   and therefore stays re-runnable per D-11, matching the existing `accounts_one_default`
   index idiom in this schema.

Every statement uses `IF NOT EXISTS` / guarded-UPDATE forms so re-applying the file is a no-op
(D-11). No DEFAULT is set on `business_id` — the migration-033 "seeded default" pattern does
not apply here, because MAP-04 explicitly wants unmatched accounts to remain unlinked.
  </action>

  <verify>
    <automated>test -f dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql &amp;&amp; grep -cE '^-- (WHAT|WHY|ROLLBACK|BACKFILL):' dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql | grep -qx 4 &amp;&amp; echo HEADER_OK</automated>
    <automated>M=dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql; P="docker exec -i mig057probe psql -U postgres -d mailbox -q -v ON_ERROR_STOP=1"; docker run --rm -d --name mig057probe -e POSTGRES_PASSWORD=p -e POSTGRES_DB=mailbox postgres:17-alpine >/dev/null; sleep 8; cat dashboard/test/fixtures/schema.sql | $P >/dev/null &amp;&amp; echo "INSERT INTO mailbox.businesses (name) VALUES ('Altitude Guitar'),('UMB Advisors'),('Dup!! Name'),('Dup Name');" | $P >/dev/null &amp;&amp; cat $M | $P &amp;&amp; cat $M | $P &amp;&amp; docker exec mig057probe psql -U postgres -d mailbox -tAc "SELECT name||'='||slug FROM mailbox.businesses ORDER BY id;"; docker rm -f mig057probe >/dev/null</automated>
  </verify>

  <acceptance_criteria>
    - File exists at the exact path `dashboard/migrations/057-add-accounts-business-id-and-business-slug-v1-2026-07-28.sql`; no other new file lands in `dashboard/migrations/`.
    - Header contains all four of the `WHAT:` / `WHY:` / `ROLLBACK:` / `BACKFILL:` labelled lines and cites the number 057 (not a copied stale number from a neighbouring file).
    - Applying the file twice in a row against the same database succeeds both times with no error (D-11 re-runnable).
    - `mailbox.accounts.business_id` is nullable with no DEFAULT and no NOT NULL constraint.
    - `mailbox.businesses.slug` is NOT NULL and covered by the unique index `businesses_slug_key`.
    - Against the probe data above, the row named `Altitude Guitar` ends with slug `altitude`; `UMB Advisors` ends with `umb-advisors`; the two colliding names (`Dup!! Name` / `Dup Name`, both transforming to `dup-name`) end with `dup-name` and `dup-name-2` respectively.
    - No statement in the file references `jobs.json`, and no seed statement targets any slug other than `altitude`.
  </acceptance_criteria>

  <done>Migration 057 exists, applies cleanly and idempotently against a fresh fixture-bootstrapped Postgres, produces the six expected live-data slugs, resolves a synthetic collision to `-2`, and seeds `altitude` on exactly one row.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Schema sync — mirror 057 into the fixture snapshot, regenerate kysely-codegen, verify no drift</name>

  <read_first>
    - `dashboard/test/fixtures/schema.sql` lines 1228-1245 (the `mailbox.accounts` CREATE TABLE and its `accounts_one_default` index)
    - `dashboard/test/fixtures/schema.sql` lines 1596-1615 (the `mailbox.businesses` and `mailbox.departments` CREATE TABLE blocks)
    - `dashboard/test/fixtures/schema.sql` last 12 lines (the `-- ── Migration 056 (orig 050) ──` tail block — the append convention this task follows)
    - `dashboard/scripts/codegen-db.sh` — what `db:codegen` and `db:codegen:verify` actually do
    - `dashboard/lib/db/schema.ts` — the generated `Accounts` interface, specifically how the existing nullable `display_label` column is typed, which is the shape `business_id` must take
  </read_first>

  <files>dashboard/test/fixtures/schema.sql, dashboard/lib/db/schema.ts</files>

  <action>
This is the phase's highest-risk task and the reason this plan is `autonomous: false`. Four
things must all be true or the phase ships silently broken: (1) the migration exists, (2) the
same DDL is mirrored into the fixture snapshot, (3) `dashboard/lib/db/schema.ts` is regenerated
from that snapshot, (4) drift is checked by hand. CI applies `test/fixtures/schema.sql` — not
`migrations/` — and CI does NOT run `db:codegen:verify` despite what the script comment
implies, so nothing else in the pipeline catches a partial sync.

**Fixture mirror.** Append a new block at the END of `dashboard/test/fixtures/schema.sql`,
after the existing `-- ── Migration 056 (orig 050): thread_id indexes ──` section, matching that
file's established tail-append convention:

```
-- ── Migration 057: M5 unified entities — accounts.business_id + businesses.slug ──
ALTER TABLE mailbox.accounts    ADD COLUMN IF NOT EXISTS business_id INTEGER
  REFERENCES mailbox.businesses(id) ON DELETE SET NULL;
ALTER TABLE mailbox.businesses  ADD COLUMN IF NOT EXISTS slug TEXT NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS businesses_slug_key ON mailbox.businesses (slug);
```

Both columns go in this trailing block, NOT inside their `CREATE TABLE` bodies. For
`business_id` this is mandatory, not stylistic: `mailbox.accounts` is defined at ~:1228 but the
FK target `mailbox.businesses` is not created until ~:1599, so an inline FK reference fails at
bootstrap (PATTERNS conflict #1). Keeping `slug` in the same trailing block is a deliberate
consistency call so the whole 057 mirror is one reviewable hunk. `ADD COLUMN ... NOT NULL` with
no DEFAULT is legal here precisely because the fixture bootstraps an empty database — there are
zero rows to violate the constraint, which is why the fixture does not need the migration's
add-nullable→backfill→constrain dance.

Do NOT copy the migration's backfill UPDATE, its PL/pgSQL collision loop, or its `altitude`
seed into the fixture. The fixture represents final-state DDL for a fresh database; carrying
DML into it would seed phantom rows into every CI run and every codegen container.

**Codegen.** Run `npm run db:codegen` from `dashboard/`. It spins a throwaway
`postgres:17-alpine` container, applies the fixture, and rewrites `dashboard/lib/db/schema.ts`.
Confirm the regenerated `Accounts` interface gained `business_id` typed as a nullable integer
(kysely-codegen's own shape for a nullable FK — compare it against the existing
`display_label: string | null` on the same interface). This matters because plan 05-02's
`linkAccountToBusiness` writes `accounts.business_id` through Kysely; that write is deliberately
Kysely rather than raw `pg` so a stale codegen artifact becomes a loud `tsc` failure rather than
silent schema drift.

**If Docker is unavailable** in the execution environment: do not block the phase. Hand-edit
`dashboard/lib/db/schema.ts`'s `Accounts` interface to add `business_id` with exactly the shape
kysely-codegen emits for a nullable integer column (mirror the neighbouring nullable columns in
that same generated interface), record in the plan SUMMARY that codegen was hand-stubbed, and
carry a follow-up item to re-run `npm run db:codegen` before the plan 05-04 live deploy. Do not
mark this task complete silently under the fallback — the SUMMARY must say which path was taken.

**Drift check.** Run `npm run db:codegen:verify` by hand and confirm it reports no diff. Then
run `npm run typecheck` — it must pass with the new column present.
  </action>

  <verify>
    <automated>grep -c 'Migration 057' dashboard/test/fixtures/schema.sql | grep -qx 1 && grep -q 'businesses_slug_key' dashboard/test/fixtures/schema.sql && echo FIXTURE_OK</automated>
    <automated>grep -q 'business_id' dashboard/lib/db/schema.ts && echo CODEGEN_HAS_COLUMN</automated>
    <automated>cd dashboard && npm run db:codegen:verify</automated>
    <automated>cd dashboard && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `dashboard/test/fixtures/schema.sql` ends with a single `-- ── Migration 057 ... ──` block containing exactly the two `ALTER TABLE` statements and the one `CREATE UNIQUE INDEX`.
    - The `mailbox.accounts` CREATE TABLE block (~:1228) is byte-for-byte unchanged — no `business_id` was inlined there.
    - The `mailbox.businesses` CREATE TABLE block (~:1599) is byte-for-byte unchanged — no `slug` was inlined there.
    - No backfill UPDATE, PL/pgSQL block, or `altitude` seed appears anywhere in the fixture file.
    - `dashboard/lib/db/schema.ts` contains `business_id` on the `Accounts` interface, typed nullable, matching the neighbouring generated nullable columns' shape.
    - `npm run db:codegen:verify` exits 0.
    - `npm run typecheck` exits 0.
    - If the Docker fallback was used, the plan SUMMARY explicitly records "codegen hand-stubbed" plus the follow-up to re-run codegen before plan 05-04.
  </acceptance_criteria>

  <done>The fixture snapshot, the migration, and the generated Kysely types all describe the same schema; `db:codegen:verify` and `typecheck` both pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema-invariant coverage for the FK and slug constraints, and repair the existing business INSERTs that slug NOT NULL breaks</name>

  <read_first>
    - `dashboard/test/schema-invariants.test.ts` lines 1-100 — the `DB_URL` gate, the conditional `beforeAll` pool creation, the `if (!pool) return;` early-return idiom used inside each `it()`, and the `getIndexDef` helper
    - `dashboard/test/lib/job-outcomes.test.ts` lines 18-45 — the two raw `INSERT INTO mailbox.businesses (name)` calls and the `STAMP` uniquing convention
    - `dashboard/test/helpers/db.ts` — `HAS_DB` / `getTestPool` / `closeTestPool`
  </read_first>

  <files>dashboard/test/schema-invariants.test.ts, dashboard/test/lib/job-outcomes.test.ts</files>

  <behavior>
    - Given a business row and an account row linked to it, when the business is deleted, then the account row still exists and its `business_id` is NULL (MAP-01 `ON DELETE SET NULL`, never cascade).
    - `information_schema.columns` reports `mailbox.accounts.business_id` as nullable with no column default.
    - `information_schema.columns` reports `mailbox.businesses.slug` as NOT NULL.
    - `pg_indexes` contains a unique index named `businesses_slug_key` on `mailbox.businesses (slug)`.
    - Inserting two businesses with the same slug is rejected by the database (unique violation).
    - `test/lib/job-outcomes.test.ts` continues to pass unchanged in behavior against a schema where `businesses.slug` is NOT NULL.
  </behavior>

  <action>
Write the assertions first, watch them fail against the pre-057 schema, then confirm they pass
after applying 057 — the migration already exists from Task 1, so the RED step here is running
the new cases against a database that has not had 057 applied.

**Extend `dashboard/test/schema-invariants.test.ts`.** Follow that file's existing structure
exactly: it holds a module-level `DB_URL`, creates `pool` in `beforeAll` only when `DB_URL` is
set, and every DB-touching `it()` opens with an early return when `pool` is undefined so the
suite stays green on a machine with no Postgres. Match that idiom — do not introduce a second
gating convention.

Add cases covering the `<behavior>` list. For the `ON DELETE SET NULL` case, prefer the
behavioral proof over a catalog lookup: insert a uniquely-stamped business, insert a uniquely-
stamped account row linked to it, delete the business, then re-select the account and assert the
row is still present with a null `business_id`. Use a timestamp+random stamp for both the
business name/slug and the account email so this suite cannot collide with the other suites that
share the same database, and clean up the account row in a `finally` (the business is already
gone by assertion time).

For the constraint cases, query `information_schema.columns` for `is_nullable` and
`column_default`, and reuse the file's existing `getIndexDef` helper for `businesses_slug_key` —
asserting the returned definition is a UNIQUE index over `slug`.

For the unique-violation case, insert one stamped business then attempt a second insert reusing
the same slug with a different name, and assert the promise rejects.

**Repair `dashboard/test/lib/job-outcomes.test.ts`.** Its `beforeAll` raw-inserts two businesses
supplying only `name`, which now violates the NOT NULL slug constraint. Add a slug value to both
inserts — a lowercased kebab derivation of the same stamped name, so the values stay unique per
run exactly like the names already are. Change nothing else in that file; its assertions are
about job outcomes, not businesses.

Before finishing, re-grep the whole `dashboard/` tree for any other writer that inserts a
business row without a slug (application code, scripts, or tests) and repair each the same way.
Two are known (`job-outcomes.test.ts` lines ~26 and ~31) plus `lib/crm/queries.ts:createBusiness`,
which plan 05-02 Task 3 owns — do not touch `lib/crm/queries.ts` in this plan, it belongs to a
different wave and would create a file conflict.
  </action>

  <verify>
    <automated>cd dashboard &amp;&amp; npx vitest run test/schema-invariants.test.ts test/lib/job-outcomes.test.ts</automated>
    <automated>grep -rn "INTO mailbox.businesses" dashboard --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "slug" | grep -v "lib/crm/queries.ts" | wc -l | grep -qx 0 &amp;&amp; echo NO_SLUGLESS_INSERTS</automated>
  </verify>

  <acceptance_criteria>
    - New `it()` cases exist in `dashboard/test/schema-invariants.test.ts` covering every bullet in `<behavior>`, each using the file's existing `if (!pool) return;` gate so the suite stays green with no Postgres available.
    - The `ON DELETE SET NULL` case proves behavior (delete the business, re-read the account) rather than only reading catalog metadata.
    - All test data is uniquely stamped and cleaned up; running the suite twice in a row against the same database passes both times.
    - `dashboard/test/lib/job-outcomes.test.ts` supplies a unique slug on both business inserts and its existing assertions are otherwise untouched.
    - The grep gate confirms no remaining slug-less business INSERT outside `lib/crm/queries.ts` (owned by plan 05-02).
    - `dashboard/lib/crm/queries.ts` is NOT modified by this plan.
  </acceptance_criteria>

  <done>Schema-invariant tests prove the FK sets null on business delete and that slug is NOT NULL + unique; every pre-existing business INSERT in the tree has been made slug-aware except the one plan 05-02 owns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → live appliance DB | Migration 057 DDL crosses from source control into `agentbox3`'s Postgres. Applied in plan 05-04 behind a blocking human gate; this plan only authors it. |
| fixture snapshot → generated types | `test/fixtures/schema.sql` is the sole input to kysely-codegen and to CI's database. A wrong fixture silently mis-shapes every downstream typecheck. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | migration 057 DDL applied to live `mailbox` DB | high | mitigate | Every statement uses `IF NOT EXISTS`/guarded-UPDATE forms and runs inside `runner.ts`'s single `BEGIN`/`COMMIT`, so any failure rolls the whole file back. No `DROP`, no column rename, no data deletion anywhere in the file. Task 1's verify applies the file twice against a throwaway container to prove idempotence before it ever reaches live. |
| T-05-02 | Tampering | `businesses.slug` unique index vs. the `altitude` seed | medium | mitigate | The seed is guarded by `NOT EXISTS (SELECT 1 ... WHERE slug = 'altitude')` and is ordered before the unique index creation, so a defeated guard fails index creation and rolls back rather than persisting a duplicate. |
| T-05-03 | Denial of Service | `businesses.slug` NOT NULL breaking existing writers | high | mitigate | Task 3 greps the tree for every slug-less business INSERT and repairs each; `lib/crm/queries.ts:createBusiness` (the live `POST /api/crm/businesses` path) is explicitly handed to plan 05-02 Task 3, which is a hard `depends_on` of this plan's wave. A missed writer surfaces as a 23502 at runtime, so the grep gate in Task 3's verify is the control. |
| T-05-04 | Information Disclosure | schema-invariant test data in a shared database | low | accept | Test rows are uniquely stamped synthetic values on a single-tenant appliance-shaped database; no real customer data is written or read. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds zero packages. `05-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide finding: no new dependency, so no `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
1. `cd dashboard && npm run lint` — biome, first in the real CI order.
2. `cd dashboard && npm run typecheck` — must pass with `business_id` present in the generated types.
3. `cd dashboard && npm run db:codegen:verify` — no drift between the fixture and `lib/db/schema.ts`.
4. `cd dashboard && npm test` — full suite; DB-gated cases skip without `TEST_POSTGRES_URL`.
5. With Postgres available: `cd dashboard && TEST_POSTGRES_URL=<url> npx vitest run test/schema-invariants.test.ts test/lib/job-outcomes.test.ts` — the new FK and slug cases actually execute rather than early-returning.
</verification>

<success_criteria>
- Migration 057 exists, is idempotent, and produces `accounts.business_id` (nullable, ON DELETE SET NULL) plus `businesses.slug` (NOT NULL, uniquely indexed) with `Altitude Guitar` carrying `altitude`.
- `test/fixtures/schema.sql` and `lib/db/schema.ts` agree with the migration; `db:codegen:verify` is clean.
- Deleting a business provably nulls its accounts' `business_id` instead of deleting the account.
- No existing business INSERT in the tree is left slug-less except the one plan 05-02 owns.
- `npm run lint && npm run typecheck && npm test` all pass in that order.
</success_criteria>

<output>
Create `.planning/phases/05-backend-data-model-auto-create/05-01-SUMMARY.md` when done.
Record explicitly whether `npm run db:codegen` ran for real or was hand-stubbed under the Docker fallback.
</output>
