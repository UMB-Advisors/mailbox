---
phase: 05-backend-data-model-auto-create
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified: []
autonomous: false
requirements: [MAP-04, FILT-05]

user_setup:
  - service: agentbox3
    why: "Live appliance hosting the M5 Postgres and dashboard; the migration and backfill must be applied there for MAP-04 and FILT-05 to be satisfiable"
    env_vars: []
    dashboard_config:
      - task: "Confirm SSH reachability over Tailscale before starting (ssh thumbox3@agentbox3)"
        location: "Local terminal — Tailscale must be up on both ends"
      - task: "Confirm the PR #239 rollback artifacts are still on the box: the pre-unified-20260711 image and the accompanying DB dump"
        location: "agentbox3 — docker image list and the deploy dump directory"

must_haves:
  truths:
    - "Migration 057 is recorded in mailbox.migrations on the live agentbox3 database (MAP-01, FILT-05)"
    - "All six live accounts carry a non-null business_id — zero unlinked accounts (MAP-04, D-09)"
    - "The three missing businesses exist: Jiffy Auto Glass, Elevated Advisory, Bonvillian Design (D-09)"
    - "The three pre-existing businesses (Altitude Guitar, UMB Advisors, AutoCSR) were reused, not duplicated (ENT-02, D-09)"
    - "Every live business row has a non-null unique slug, and Altitude Guitar's is exactly 'altitude' (FILT-05, D-14)"
    - "The live ~/.hermes/cron/jobs.json still carries exactly one business slug, and it is 'altitude' — the D-14 assumption is re-verified against reality, not assumed (RESEARCH A2)"
    - "The dashboard container runs an image built from the merged Phase 5 source, so the runtime auto-link hook is actually live"
    - "A rollback path was confirmed to exist before any DDL touched the live database"
  artifacts:
    - "A recorded row in mailbox.migrations with version 057-add-accounts-business-id-and-business-slug-v1-2026-07-28"
    - "A pre-migration pg_dump of the live mailbox database, retained on agentbox3"
    - "The post-backfill account-to-business join output, captured verbatim in the plan SUMMARY"
  key_links:
    - "migration apply ↔ dashboard image rebuild — the dashboard runs from a baked image on agentbox3, so applying the migration without rebuilding leaves the old code running against the new schema"
    - "live slug seed ↔ Phase 7 sidecar deploy — the ROADMAP cross-repo gate: Phase 7 must not ship until 'altitude' is verified present here, and there is no compile-time signal if that order is violated"
    - "jobs.json live state ↔ D-14's single-row seed — if the file drifted since 2026-07-28 the seed target changes, which is why this plan verifies rather than assumes"
  prohibitions:
    - "MUST NOT target the mailbox1 Heron appliance. M5 is Mike's agentbox3 fork. CLAUDE.md's Deployment Target section documents mailbox1 and does NOT apply to this phase (directive: deploy target note)."
    - "MUST NOT apply DDL to the live database before the blocking checkpoint in Task 1 is explicitly approved"
    - "MUST NOT edit, move, or rewrite ~/.hermes/cron/jobs.json — this plan reads it only (D-14)"
    - "MUST NOT seed any slug other than 'altitude'; specifically not the 11 sidecar ENTITY_OPTIONS slugs, which belong to Heron Labs' customer list rather than Mike's businesses (D-15)"
    - "MUST NOT deploy anything to UMB-Advisors/agentbox-sidecar from this plan — that is Phase 7 (directive 7)"
    - "MUST NOT hand-write a business row or a business_id value directly in SQL to 'fix' an account the backfill left unlinked — investigate why the rule missed it and report it instead, so the runtime hook gets fixed rather than papered over"
---

<objective>
Apply Phase 5 to the live `agentbox3` appliance and prove the two requirements that can only be
satisfied against real data: MAP-04 (the six live accounts linked, the three missing businesses
created) and FILT-05's live half (every business has a slug, and `Altitude Guitar` carries
`altitude` so the one live cron job keeps resolving).

Purpose: the ROADMAP records a cross-repo gate — Phase 7's sidecar build must not deploy until
Phase 5 is deployed and verified live, and the two repos share no CI, so nothing catches a
violated ordering at build time. This plan produces the evidence that gate depends on.

Output: migration 057 recorded on the live database, six linked accounts, six slugged
businesses, and the verbatim verification output captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-data-model-auto-create/05-CONTEXT.md
@.planning/phases/05-backend-data-model-auto-create/05-01-SUMMARY.md
@.planning/phases/05-backend-data-model-auto-create/05-02-SUMMARY.md
@.planning/phases/05-backend-data-model-auto-create/05-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_plan_produces>
**No repository files are created or modified by this plan.** Its outputs are live-appliance
state and recorded evidence.

**Live state produced on `agentbox3`**
- Row in `mailbox.migrations`: `057-add-accounts-business-id-and-business-slug-v1-2026-07-28`
- `mailbox.accounts.business_id` populated on all six live accounts
- `mailbox.businesses` rows created: `Jiffy Auto Glass`, `Elevated Advisory`, `Bonvillian Design`
- `mailbox.businesses.slug` populated on all six rows, with `Altitude Guitar` = `altitude`
- Rebuilt `mailbox-dashboard` image carrying the Phase 5 source

**Evidence captured into `05-04-SUMMARY.md`**
- Pre-migration `pg_dump` path and the rollback image tag confirmed present
- The `BACKFILL_DRY_RUN=1` output
- The final account-to-business join output
- The live `jobs.json` slug-reference finding
</artifacts_this_plan_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight gate — confirm rollback path and re-verify the jobs.json assumption before any live DDL</name>

  <read_first>
    - `.planning/phases/05-backend-data-model-auto-create/05-CONTEXT.md` § D-09 (the expected six accounts and three businesses), § D-14 (the single-slug jobs.json claim)
    - `.planning/phases/05-backend-data-model-auto-create/05-RESEARCH.md` § "Assumptions Log" A1 and A2 — the two live-state claims this task exists to convert from assumed to verified
    - `.planning/phases/05-backend-data-model-auto-create/05-03-SUMMARY.md` — the exact backfill command line
  </read_first>

  <files>none — this task is read-only against the live appliance except for taking a pg_dump</files>

  <action>
Do not execute any DDL against the live database in this task. Run the read-only inspection
steps in `<how-to-verify>` below, take the pre-migration dump, and present the collected output
to Mike for approval. Two assumptions from research (A1: the container/image layout on
`agentbox3`; A2: `jobs.json` holding exactly one `altitude` slug) were user-asserted rather than
independently probed — this task converts both into verified facts before anything irreversible
happens. Report divergences rather than adapting silently: a different account or business set
than D-09 recorded changes the expected backfill outcome, and drifted `jobs.json` slugs change
what FILT-05's seed has to cover.
  </action>

  <what-built>
    Plans 05-01 through 05-03 produced, and proved green in CI order against a local
    fixture-bootstrapped Postgres: migration 057 (`accounts.business_id` FK plus
    `businesses.slug` with its computed backfill and the single `altitude` seed), the mirrored
    fixture snapshot and regenerated Kysely types, `lib/crm/auto-link.ts` holding the whole
    resolution rule, the `persistAccountLink` seam wired into all three account creators, and
    `npm run business:backfill`. Nothing has touched the live appliance yet.
  </what-built>

  <how-to-verify>
    Run each step and paste the output back. Everything here is read-only except the dump in
    step 4.

    1. Confirm reachability and identity of the target. This is `agentbox3`, NOT `mailbox1`:
       `ssh thumbox3@agentbox3 'hostname && uptime'`

    2. Confirm the containers and the database name:
       `ssh thumbox3@agentbox3 'docker ps --format "{{.Names}}\t{{.Image}}"'`
       Expect `mailbox-postgres-1` and a `mailbox-dashboard` container. Note the dashboard's
       current image tag — that is the thing being replaced.

    3. Confirm the PR #239 rollback artifacts are still present:
       `ssh thumbox3@agentbox3 'docker image ls | grep -i pre-unified-20260711'`
       plus whatever path holds the accompanying DB dump from that deploy. If either is missing,
       stop and say so — step 4 becomes the only rollback path and that changes the risk picture.

    4. Take a fresh pre-migration dump and report its path and size:
       `ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 pg_dump -U mailbox -d mailbox' > ~/agentbox3-mailbox-pre057.sql`
       Confirm the file is non-trivially sized rather than a few empty bytes.

    5. Report the current live state so the backfill's expected outcome is pinned to reality
       rather than to a two-week-old note:
       `ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -c "SELECT id, email_address, display_label FROM mailbox.accounts ORDER BY id;"'`
       `ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -c "SELECT id, name FROM mailbox.businesses ORDER BY id;"'`
       D-09 expects six accounts and three businesses (`Altitude Guitar`, `UMB Advisors`,
       `AutoCSR`). If the counts or names differ, say so — the expected outcome shifts and I
       will recompute it before proceeding.

    6. Re-verify the D-14 assumption directly, because a wrong answer here changes what gets
       seeded:
       `ssh thumbox3@agentbox3 'cat ~/.hermes/cron/jobs.json'`
       Confirm how many jobs exist and how many carry a business slug. D-14 recorded four jobs
       with exactly one slug, `altitude`. If the file has drifted, report the actual slugs — do
       not edit the file.

    7. Confirm the migration list on the live database currently ends at 056:
       `ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -c "SELECT version FROM mailbox.migrations ORDER BY version DESC LIMIT 3;"'`
  </how-to-verify>

  <verify>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT count(*) FROM mailbox.accounts;"'</automated>
    <automated>ssh thumbox3@agentbox3 'test -f ~/.hermes/cron/jobs.json &amp;&amp; grep -o "altitude" ~/.hermes/cron/jobs.json | wc -l'</automated>
  </verify>

  <acceptance_criteria>
    - The target host is confirmed as `agentbox3`, reached as user `thumbox3` over Tailscale; `mailbox1` was not contacted.
    - `mailbox-postgres-1` and the dashboard container are both confirmed running, and the dashboard's current image tag is recorded.
    - Either the `pre-unified-20260711` rollback image is confirmed present, or its absence is explicitly reported and acknowledged before proceeding.
    - A fresh pre-migration `pg_dump` exists locally at a recorded path with a plausible non-empty size.
    - The live account and business rows are recorded verbatim, and any divergence from D-09's expected six-and-three is surfaced rather than glossed over.
    - The live `jobs.json` slug content is reported, and any drift from D-14's single `altitude` reference is surfaced. The file is unmodified.
    - The live migration ledger is confirmed to end at 056, so 057 is genuinely the next version.
    - No DDL has been executed against the live database at the end of this task.
  </acceptance_criteria>

  <resume-signal>Type "approved" to proceed to the live migration apply, or paste the divergence you found (different account/business rows, drifted jobs.json, missing rollback image) and I will recompute the expected outcome before touching anything.</resume-signal>

  <done>The target is confirmed to be agentbox3, a rollback path exists, a fresh pre-migration dump is on disk, the live account/business/migration state and the jobs.json slug content are recorded, and Mike has explicitly approved proceeding to live DDL.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Phase 5 to agentbox3 — rebuild the dashboard image and apply migration 057</name>

  <read_first>
    - `.planning/phases/05-backend-data-model-auto-create/05-04-PLAN.md` Task 1 output (the recorded live state and image tag from the checkpoint)
    - `dashboard/migrations/runner.ts` — confirms each migration file runs inside one `BEGIN`/`COMMIT`, so a mid-file failure rolls the whole file back and leaves the ledger unchanged
    - `CLAUDE.md` § "Deploy flow" — the general pull/rebuild shape, noting the host, alias, and repo path documented there are `mailbox1`'s and must be substituted for `agentbox3`'s
  </read_first>

  <files>none — this task changes live appliance state only</files>

  <action>
Only run this after Task 1's checkpoint is explicitly approved.

Get the Phase 5 source onto the box and rebuild. The dashboard on `agentbox3` runs from a baked
Docker image, so a `git pull` alone changes nothing at runtime — the image must be rebuilt or
the migration lands against a schema the running code does not know about. Pull the branch
carrying plans 05-01 through 05-03, then bring the stack up with a build. Pass
`--remove-orphans` on any full-stack `up`, per the standing repo rule that a removed service
otherwise keeps its host port binding.

Before rebuilding, record what the box is actually on — branch and short SHA. The STAQPRO-336
precedent is a 36-hour rebuild burned because an appliance was silently on a different branch
than assumed. Confirm the checkout matches what was verified locally in plan 05-03 Task 3.

Apply migration 057 through the repo's own runner rather than by piping SQL into `psql`. The
runner records the version in `mailbox.migrations`, sorts lexically, skips already-applied
files, and wraps each file in a transaction — all of which are lost if the file is applied by
hand. Use the migrate compose profile:
`docker compose --profile migrate run --rm mailbox-migrate`.

Then confirm three things immediately, before the backfill:

- `mailbox.migrations` now contains `057-add-accounts-business-id-and-business-slug-v1-2026-07-28`
  and nothing unexpected beyond it.
- `mailbox.accounts` has a `business_id` column, nullable, and every existing row still holds
  null — the migration deliberately links nothing, which is what leaves the linking to the
  script that shares the runtime rule (D-10).
- `mailbox.businesses` has a `slug` on every row, and the `Altitude Guitar` row's slug is exactly
  `altitude` rather than `altitude-guitar` — proving the D-14 seed ran after the generic backfill
  rather than being overwritten by it.

If the migration fails, it rolled back as a unit and the ledger is unchanged; report the error
and stop rather than retrying blind or hand-patching SQL.
  </action>

  <verify>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT count(*) FROM mailbox.migrations WHERE version LIKE '"'"'057-%'"'"';"' | grep -qx 1 &amp;&amp; echo MIGRATION_RECORDED</automated>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT count(*) FROM mailbox.businesses WHERE slug IS NULL;"' | grep -qx 0 &amp;&amp; echo ALL_BUSINESSES_SLUGGED</automated>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT slug FROM mailbox.businesses WHERE name = '"'"'Altitude Guitar'"'"';"' | grep -qx altitude &amp;&amp; echo LEGACY_SLUG_SEEDED</automated>
    <automated>ssh thumbox3@agentbox3 'docker ps --filter name=mailbox-dashboard --format "{{.Status}}"' | grep -qi up &amp;&amp; echo DASHBOARD_UP</automated>
  </verify>

  <acceptance_criteria>
    - The box's branch and short SHA were recorded before the rebuild and match what plan 05-03 verified locally.
    - The dashboard image was rebuilt and its container is running, so the runtime auto-link hook is live rather than dormant in an unbuilt source tree.
    - Migration 057 was applied through the migrate compose profile, not by hand-piping SQL, and is recorded in `mailbox.migrations`.
    - `mailbox.accounts.business_id` exists and is null on every row at the end of this task — the migration links nothing on its own.
    - Every row in `mailbox.businesses` has a non-null slug and `Altitude Guitar`'s is exactly `altitude`.
    - `~/.hermes/cron/jobs.json` is unmodified.
    - No sidecar repo was touched.
  </acceptance_criteria>

  <done>The live appliance runs Phase 5 code against a Phase 5 schema; migration 057 is recorded, every business is slugged, and the legacy `altitude` slug is live.</done>
</task>

<task type="auto">
  <name>Task 3: Run the backfill and verify the full MAP-04 / FILT-05 end state</name>

  <read_first>
    - `.planning/phases/05-backend-data-model-auto-create/05-03-SUMMARY.md` — the exact `business:backfill` command line and its dry-run flag
    - `.planning/phases/05-backend-data-model-auto-create/05-CONTEXT.md` § D-09 — the expected mapping: three accounts link to existing businesses, three new businesses get created
    - Task 1's recorded live account and business rows — the actual expected outcome, which supersedes D-09 if the two diverge
  </read_first>

  <files>none — this task changes live appliance state only</files>

  <action>
Run the backfill in dry-run first:
`docker exec -e BACKFILL_DRY_RUN=1 mailbox-dashboard npm run business:backfill`
(matching the `docker exec mailbox-dashboard npx tsx scripts/...` shape this repo already uses
for `rag-backfill`). Read the output against the expected mapping before writing anything. Every
one of the six accounts should show a resolution path — either an existing business matched by
name or by a sibling on the same domain, or a new business to be created. Three should resolve
to the existing `Altitude Guitar`, `UMB Advisors`, and `AutoCSR`; three should propose creating
`Jiffy Auto Glass`, `Elevated Advisory`, and `Bonvillian Design`. If any account resolves
somewhere unexpected, stop and report it — that is a defect in the runtime rule, and it will
misbehave on the next real connect too. Do not hand-fix the row.

When the dry run reads correctly, run it for real:
`docker exec mailbox-dashboard npm run business:backfill`
It exits non-zero if any account is left unlinked, so treat a non-zero exit as a failure to
investigate rather than a warning to note.

Then run it a second time and confirm it reports zero accounts processed and creates no rows —
that is the D-11 re-runnability proof, and it is cheap to get here on real data.

Capture the end state verbatim into the SUMMARY:

- The account-to-business join:
  `SELECT a.id, a.email_address, a.display_label, b.id AS business_id, b.name, b.slug FROM mailbox.accounts a LEFT JOIN mailbox.businesses b ON b.id = a.business_id ORDER BY a.id;`
- The unlinked count, which must be zero:
  `SELECT count(*) FROM mailbox.accounts WHERE business_id IS NULL;`
- The full business list with slugs, which must show six rows, all slugs distinct and non-null,
  with `Altitude Guitar` on `altitude`:
  `SELECT id, name, slug FROM mailbox.businesses ORDER BY id;`

Finally, sanity-check the runtime hook rather than only the backfill — they share the rule but
run through different entry points. Confirm the dashboard is serving and that
`GET /api/crm/businesses` returns the six businesses including their slugs, which is also the
exact read Phase 7's sidecar will consume. Record that response in the SUMMARY as the artifact
the ROADMAP's cross-repo gate depends on.
  </action>

  <verify>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT count(*) FROM mailbox.accounts WHERE business_id IS NULL;"' | grep -qx 0 &amp;&amp; echo ZERO_UNLINKED</automated>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT count(DISTINCT slug) = count(*) AND count(*) FILTER (WHERE slug IS NULL) = 0 FROM mailbox.businesses;"' | grep -qx t &amp;&amp; echo SLUGS_UNIQUE_AND_PRESENT</automated>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-postgres-1 psql -U mailbox -d mailbox -tAc "SELECT string_agg(name, '"'"','"'"' ORDER BY name) FROM mailbox.businesses;"'</automated>
    <automated>ssh thumbox3@agentbox3 'docker exec mailbox-dashboard npm run business:backfill' &amp;&amp; echo RERUN_CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The dry run was executed and read before any write, and its output is recorded in the SUMMARY.
    - After the real run, `SELECT count(*) FROM mailbox.accounts WHERE business_id IS NULL` returns 0 — every live account is linked (MAP-04, D-09).
    - `mailbox.businesses` contains the three pre-existing rows reused (not duplicated) plus `Jiffy Auto Glass`, `Elevated Advisory`, and `Bonvillian Design` newly created.
    - Every business slug is non-null and distinct, and `Altitude Guitar` carries `altitude` (FILT-05, D-14).
    - A second consecutive backfill run reports zero accounts processed, creates no rows, and exits zero (D-11).
    - `GET /api/crm/businesses` returns all six businesses with their slugs, and the response is recorded in the SUMMARY as the Phase 7 gate evidence.
    - No account was linked by hand-written SQL; any account the rule failed to resolve was reported as a defect rather than patched.
    - `~/.hermes/cron/jobs.json` is still unmodified, and no slug other than `altitude` was seeded.
  </acceptance_criteria>

  <done>All six live accounts are linked to businesses by the same rule the runtime hook uses, the three missing businesses exist, every business carries a unique slug with the legacy `altitude` preserved, and the backfill is proven re-runnable against real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → agentbox3 | SSH over Tailscale as `thumbox3`. The operator's own single-tenant appliance; no new exposure is created by this plan. |
| repo source → live Postgres | Migration 057's DDL and the backfill's DML cross into production data here. This is the only irreversible surface in the phase, which is why Task 1 is a blocking gate. |
| dashboard container → docker network | Unchanged. This phase adds no HTTP route and alters no auth posture; Caddy `basic_auth` still fronts every path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Tampering | irreversible DDL or data change on the live appliance database | high | mitigate | Task 1 is a blocking human checkpoint that confirms the `pre-unified-20260711` rollback image and takes a fresh `pg_dump` before any DDL. Migration 057 is additive only — no DROP, no rename, no delete — and `runner.ts` wraps it in one transaction, so a partial apply is impossible. |
| T-05-16 | Tampering | deploying to the wrong appliance (`mailbox1`, the live Heron Labs customer box) | high | mitigate | Task 1 step 1 confirms `hostname` before anything else, and the prohibition block names `mailbox1` explicitly as out of scope. CLAUDE.md's Deployment Target section documents `mailbox1` and is the exact trap this control exists for. |
| T-05-17 | Tampering | applying migration 057 by piping SQL rather than through the runner | medium | mitigate | Task 2 mandates the migrate compose profile. A hand-applied file leaves `mailbox.migrations` unwritten, so the next deploy re-applies it — survivable only because the file is idempotent, but it silently breaks the ledger. Verified by asserting the recorded version afterward. |
| T-05-18 | Tampering | schema applied without rebuilding the baked dashboard image | high | mitigate | Task 2 rebuilds the image before the migration and asserts the container is up; Task 3 independently confirms the live code path by reading `GET /api/crm/businesses`. Without this, the new schema runs under old code and no new connect auto-links. |
| T-05-19 | Repudiation | no record of what the backfill actually did to live data | medium | mitigate | Dry run first, then the real run, then the verbatim join output and business list captured into `05-04-SUMMARY.md`. The `business_id IS NULL` candidate filter means a re-run cannot silently overwrite a later operator re-mapping. |
| T-05-20 | Denial of Service | a failed migration leaving the appliance's email pipeline down | medium | mitigate | The migration is additive and transactional, so a failure leaves the prior schema intact. The pipeline reads no new column, so even a fully failed 057 leaves classification, drafting, and send untouched. Rollback path confirmed in Task 1 regardless. |
| T-05-21 | Information Disclosure | `pg_dump` of live mailbox data written to the workstation | medium | accept | The dump contains the operator's own email metadata on his own machine, taken deliberately as the rollback artifact. Accepted for the duration of the deploy; note its path in the SUMMARY so it can be removed once the phase is verified. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. `05-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide no-new-dependency finding, so no `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
1. `ssh thumbox3@agentbox3 'hostname'` returns the agentbox3 host — not `mailbox1`.
2. `SELECT version FROM mailbox.migrations WHERE version LIKE '057-%'` returns one row.
3. `SELECT count(*) FROM mailbox.accounts WHERE business_id IS NULL` returns 0.
4. `SELECT id, name, slug FROM mailbox.businesses ORDER BY id` returns six rows, all slugs non-null and distinct, `Altitude Guitar` on `altitude`.
5. A second `npm run business:backfill` exits zero and reports zero accounts processed.
6. `GET /api/crm/businesses` through the dashboard returns the six businesses with slugs.
7. `~/.hermes/cron/jobs.json` is byte-identical to what Task 1 recorded.
</verification>

<success_criteria>
- Migration 057 is applied and recorded on the live `agentbox3` database, through the runner, behind an approved gate, with a confirmed rollback path.
- Zero live accounts remain unlinked; the three missing businesses were created and the three existing ones were reused rather than duplicated.
- Every live business carries a unique slug, and the one legacy slug a live cron job references, `altitude`, resolves.
- The backfill is proven re-runnable against real data.
- The dashboard serves the six businesses with slugs, which is the evidence the ROADMAP's Phase 7 cross-repo gate requires.
</success_criteria>

<output>
Create `.planning/phases/05-backend-data-model-auto-create/05-04-SUMMARY.md` when done.
Include verbatim: the pre-migration dump path, the live `jobs.json` finding, the dry-run output,
the final account-to-business join, the business list with slugs, and the
`GET /api/crm/businesses` response. State explicitly whether the Phase 7 cross-repo gate is now
satisfied.
</output>
