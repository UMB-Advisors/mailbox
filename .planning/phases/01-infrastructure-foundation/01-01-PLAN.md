---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - scripts/init-db/00-schemas.sql
  - dashboard/index.html
  - dashboard/Dockerfile
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-08
  - INFRA-09

must_haves:
  truths:
    - "docker-compose.yml defines exactly 5 services: postgres, qdrant, ollama, n8n, dashboard"
    - "Service boot order enforced via depends_on with condition: service_healthy"
    - "All services have healthchecks with appropriate intervals and start_periods"
    - "Postgres initializes with a mailbox schema on first boot"
    - "Qdrant has jemalloc ARM64 workaround configured"
    - "Ollama has NO mem_limit set"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Complete 5-service stack definition"
      contains: "services:"
    - path: ".env.example"
      provides: "Template for all required environment variables"
      contains: "POSTGRES_PASSWORD"
    - path: "scripts/init-db/00-schemas.sql"
      provides: "Postgres schema initialization"
      contains: "CREATE SCHEMA"
    - path: "dashboard/index.html"
      provides: "Placeholder dashboard page"
    - path: "dashboard/Dockerfile"
      provides: "Nginx-based dashboard container"
      contains: "nginx:alpine"
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "${} variable references"
      pattern: "\\$\\{POSTGRES"
    - from: "docker-compose.yml"
      to: "scripts/init-db/00-schemas.sql"
      via: "volume mount to /docker-entrypoint-initdb.d"
      pattern: "docker-entrypoint-initdb"
---

<objective>
Create the complete Docker Compose stack configuration with all 5 services, environment variable template, Postgres schema initialization, and placeholder dashboard.

Purpose: Establishes the container orchestration foundation that every subsequent phase builds on. This is the "skeleton" of the appliance.
Output: docker-compose.yml, .env.example, .gitignore, init SQL, dashboard placeholder
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker Compose stack and environment template</name>
  <files>docker-compose.yml, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 1: Docker Compose Service Boot Order — contains verified compose YAML)
    - .planning/phases/01-infrastructure-foundation/01-CONTEXT.md (decisions D-04 through D-08)
    - CLAUDE.md (Memory Budget table, Stack Patterns by Variant)
  </read_first>
  <action>
Create `docker-compose.yml` at repo root (per D-13) with these exact 5 services:

**postgres:**
- image: `postgres:17-alpine` (per CLAUDE.md tech stack, not "16" from REQUIREMENTS.md)
- environment: `POSTGRES_USER: ${POSTGRES_USER}`, `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}`, `POSTGRES_DB: ${POSTGRES_DB}`
- volumes: `postgres_data:/var/lib/postgresql/data` (named volume per D-06), `./scripts/init-db:/docker-entrypoint-initdb.d:ro`
- healthcheck: `test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]`, interval: 5s, timeout: 5s, retries: 10, start_period: 10s
- restart: unless-stopped (per D-04)
- No ports exposed (internal only — n8n connects via compose network)

**qdrant:**
- image: `qdrant/qdrant:v1.17.1` (version-pinned per D-07)
- environment: `MALLOC_CONF: "narenas:1"` (ARM64 jemalloc workaround per INFRA-08)
- volumes: `qdrant_data:/qdrant/storage` (named volume per D-06)
- ports: `6333:6333` (REST API), `6334:6334` (gRPC)
- healthcheck: `test: ["CMD-SHELL", "wget -qO- http://localhost:6333/healthz || exit 1"]`, interval: 10s, timeout: 5s, retries: 5, start_period: 15s
- depends_on: postgres (condition: service_healthy) — per D-04 boot order
- restart: unless-stopped

**ollama:**
- image: `${OLLAMA_IMAGE:-dustynv/ollama:0.18.4-r36.4-cu126-22.04}` (configurable via env for autotag resolution)
- runtime: nvidia
- NO mem_limit, NO memory, NO deploy.resources.limits.memory (per D-08 — breaks GPU detection on Jetson unified memory)
- volumes: `ollama_models:/root/.ollama` (named volume per D-06)
- ports: `11434:11434`
- healthcheck: `test: ["CMD-SHELL", "bash -c 'cat < /dev/null > /dev/tcp/localhost/11434' 2>/dev/null || exit 1"]`, interval: 15s, timeout: 10s, retries: 5, start_period: 30s (use bash TCP check — healthcheck tool availability in dustynv image is uncertain)
- depends_on: postgres (condition: service_healthy) — per D-04 boot order (parallel with qdrant)
- restart: unless-stopped

**n8n:**
- image: `n8nio/n8n:2.14.2`
- depends_on: postgres (condition: service_healthy) — per D-04
- environment: `DB_TYPE: postgresdb`, `DB_POSTGRESDB_HOST: postgres`, `DB_POSTGRESDB_DATABASE: ${POSTGRES_DB}`, `DB_POSTGRESDB_USER: ${POSTGRES_USER}`, `DB_POSTGRESDB_PASSWORD: ${POSTGRES_PASSWORD}`, `N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY}`, `N8N_HOST: 0.0.0.0`, `N8N_PORT: 5678`, `N8N_PROTOCOL: http`
- volumes: `n8n_data:/home/node/.n8n` (named volume per D-06)
- ports: `5678:5678`
- healthcheck: `test: ["CMD-SHELL", "wget -qO- http://localhost:5678/healthz || exit 1"]`, interval: 15s, timeout: 10s, retries: 5, start_period: 40s
- restart: unless-stopped

**dashboard:**
- build: `./dashboard` (custom Dockerfile)
- ports: `3000:80`
- healthcheck: `test: ["CMD-SHELL", "wget -qO- http://localhost:80/ || exit 1"]`, interval: 10s, timeout: 5s, retries: 3, start_period: 5s
- depends_on: n8n (condition: service_healthy) — last to start per D-04
- restart: unless-stopped

**volumes section** (named volumes per D-06):
- postgres_data:
- qdrant_data:
- ollama_models:
- n8n_data:

**networks:** Use default bridge network (Claude's discretion — custom network not needed for 5 services).

Create `.env.example` at repo root (per D-05, D-13) with these exact variables:
```
# Postgres
POSTGRES_USER=mailbox
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=mailbox

# n8n
N8N_ENCRYPTION_KEY=changeme_n8n_encryption_key

# Anthropic (for Phase 2+)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Ollama image (resolve via: jetson-containers autotag ollama)
OLLAMA_IMAGE=dustynv/ollama:0.18.4-r36.4-cu126-22.04
```

Create/update `.gitignore` to include:
```
.env
```
Preserve any existing .gitignore content.
  </action>
  <verify>
    <automated>grep -c "services:" docker-compose.yml && grep -c "postgres:" docker-compose.yml && grep -c "qdrant:" docker-compose.yml && grep -c "ollama:" docker-compose.yml && grep -c "n8n:" docker-compose.yml && grep -c "dashboard:" docker-compose.yml && grep -c "POSTGRES_PASSWORD" .env.example && grep -c "mem_limit" docker-compose.yml | grep "^0$" && grep -c "MALLOC_CONF" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains exactly 5 service definitions: postgres, qdrant, ollama, n8n, dashboard
    - docker-compose.yml contains `runtime: nvidia` under ollama service
    - docker-compose.yml does NOT contain `mem_limit` anywhere (grep returns 0 matches)
    - docker-compose.yml contains `MALLOC_CONF: "narenas:1"` under qdrant environment
    - docker-compose.yml contains `condition: service_healthy` for depends_on entries
    - docker-compose.yml contains `restart: unless-stopped` for all 5 services
    - docker-compose.yml volumes section declares: postgres_data, qdrant_data, ollama_models, n8n_data
    - docker-compose.yml contains `postgres:17-alpine` (not postgres:16)
    - .env.example contains: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, N8N_ENCRYPTION_KEY, ANTHROPIC_API_KEY, OLLAMA_IMAGE
    - .gitignore contains `.env`
  </acceptance_criteria>
  <done>Docker Compose stack defines all 5 services with correct boot order, healthchecks, named volumes, and Jetson-specific settings. Environment template documents all required variables.</done>
</task>

<task type="auto">
  <name>Task 2: Create Postgres schema init and dashboard placeholder</name>
  <files>scripts/init-db/00-schemas.sql, dashboard/index.html, dashboard/Dockerfile</files>
  <read_first>
    - docker-compose.yml (verify volume mount path for init-db and dashboard build context)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 2: Postgres Schema Initialization)
  </read_first>
  <action>
Create `scripts/init-db/00-schemas.sql` (per D-13 flat structure):
```sql
-- MailBox One: Postgres Schema Initialization
-- Runs only on first volume creation via /docker-entrypoint-initdb.d/
-- n8n uses the public schema (default, no action needed)
-- Application data uses the mailbox schema

CREATE SCHEMA IF NOT EXISTS mailbox;

-- Ensure the default user has full access to both schemas
GRANT ALL ON SCHEMA mailbox TO CURRENT_USER;
GRANT ALL ON SCHEMA public TO CURRENT_USER;
```

Create `dashboard/index.html` — minimal placeholder page:
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MailBox One</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, sans-serif; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; background: #f5f5f5; color: #333; }
    .container { text-align: center; padding: 2rem; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; }
    p { color: #666; }
    .status { margin-top: 2rem; padding: 1rem; background: #e8f5e9; border-radius: 8px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>MailBox One</h1>
    <p>Email Agent Appliance</p>
    <div class="status">Dashboard placeholder — Phase 1 infrastructure verified.</div>
  </div>
</body>
</html>
```

Create `dashboard/Dockerfile`:
```dockerfile
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
```
  </action>
  <verify>
    <automated>test -f scripts/init-db/00-schemas.sql && grep -c "CREATE SCHEMA" scripts/init-db/00-schemas.sql && test -f dashboard/index.html && test -f dashboard/Dockerfile && grep -c "nginx:alpine" dashboard/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - scripts/init-db/00-schemas.sql exists and contains `CREATE SCHEMA IF NOT EXISTS mailbox;`
    - scripts/init-db/00-schemas.sql contains `GRANT ALL ON SCHEMA mailbox TO CURRENT_USER;`
    - scripts/init-db/00-schemas.sql contains `GRANT ALL ON SCHEMA public TO CURRENT_USER;`
    - dashboard/index.html exists and contains `MailBox One`
    - dashboard/Dockerfile exists and contains `FROM nginx:alpine`
    - dashboard/Dockerfile contains `COPY index.html /usr/share/nginx/html/index.html`
  </acceptance_criteria>
  <done>Postgres initializes with mailbox schema on first boot. Dashboard serves a placeholder page via nginx:alpine.</done>
</task>

</tasks>

<verification>
- `grep -c "mem_limit" docker-compose.yml` returns 0
- `grep "MALLOC_CONF" docker-compose.yml` returns `narenas:1`
- `grep "service_healthy" docker-compose.yml` returns matches for depends_on conditions
- `grep "restart: unless-stopped" docker-compose.yml` returns 5 matches
- All 6 files exist at expected paths
</verification>

<success_criteria>
Complete Docker Compose stack configuration ready for deployment on Jetson. All 5 services defined with correct boot ordering, healthchecks, named volumes, and Jetson-specific constraints (no mem_limit on Ollama, MALLOC_CONF on Qdrant). Environment template and Postgres init script ready.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
